export CONFIG_UNION_FS=m

$(BUILD_STAMP):
	$(MAKE) -C $(HEADERS_DIR) M=$(CURDIR)/$(DIR)/build $(JOBS_ARG)
	touch $@

install:
	$(MAKE) -C $(HEADERS_DIR) M=$(CURDIR)/$(DIR)/build modules_install INSTALL_MOD_PATH=$(PACKAGE_DIR) INSTALL_MOD_DIR=extra/$(MODULE)

